server:
  port: 8080
camel:
  main:
    banner-mode: "off"
  springboot:
    main-run-controller: true
bot:
  telegram:
    name: bepicky
    token: ${TG_CLIENT_TOKEN}
management:
  endpoints:
    web:
      exposure:
        include: '*'
eureka:
  instance:
    hostName: na-client-bot